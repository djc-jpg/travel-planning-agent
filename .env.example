# ──────────────────────────────────────────
# trip-agent 环境变量配置
# 复制为 .env 并填入你的真实 key
# ──────────────────────────────────────────

# ── 阿里云通义千问（DashScope） ──────────
# 控制台: https://dashscope.console.aliyun.com/
DASHSCOPE_API_KEY=<YOUR_DASHSCOPE_API_KEY>
# OPENAI 兼容配置（可选）
OPENAI_API_KEY=<YOUR_OPENAI_API_KEY>
# 可选，默认 qwen3-coder-plus
# LLM_MODEL=qwen3-coder-plus

# ── 高德地图 ─────────────────────────────
# 控制台: https://console.amap.com/  → 应用管理 → 创建新 Key（Web服务类型）
AMAP_API_KEY=<YOUR_AMAP_API_KEY>

# ── 可选配置 ─────────────────────────────
# LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
# LANGCHAIN_TRACING_V2=false

# Optional production session backend (Redis)
# REDIS_URL=redis://localhost:6379/0
# SESSION_TTL_SECONDS=1800
# SESSION_MAX_SESSIONS=1000
# Allow in-memory store/rate limit fallback in single-node deployments
# ALLOW_INMEMORY_BACKEND=false

# Optional production rate limiter backend (defaults to REDIS_URL when omitted)
# RATE_LIMIT_REDIS_URL=redis://localhost:6379/0
# RATE_LIMIT_MAX=60
# RATE_LIMIT_WINDOW=60

# CORS settings
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# CORS_ALLOW_CREDENTIALS=false

# Internal diagnostics endpoint (disabled by default)
# ENABLE_DIAGNOSTICS=false
# DIAGNOSTICS_TOKEN=<LONG_RANDOM_TOKEN>

# Optional API auth for /plan and /chat (disabled when empty)
# API_BEARER_TOKEN=<LONG_RANDOM_TOKEN>
# Allow unauthenticated API only for trusted internal networks (not recommended)
# ALLOW_UNAUTHENTICATED_API=false

# When true, disallow silent fallback from external adapters to mock data
# STRICT_EXTERNAL_DATA=false
