global:
  scrape_interval: 15s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/alert_rules.yml

scrape_configs:
  - job_name: "trip-agent-backend"
    metrics_path: /metrics/prometheus
    static_configs:
      - targets: ["host.docker.internal:8000"]
