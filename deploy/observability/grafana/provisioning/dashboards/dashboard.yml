apiVersion: 1

providers:
  - name: trip-agent
    orgId: 1
    folder: trip-agent
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
